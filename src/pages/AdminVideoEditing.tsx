import React from 'react';
import { AdminView } from '@/components/AdminView';

// Simple page that mounts AdminView; AdminView reads the pathname and will initialize course context
const AdminVideoEditing = () => {
  return <AdminView />;
};

export default AdminVideoEditing;
